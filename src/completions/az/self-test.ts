// az self-test completion spec for CLIFlow
// Auto-converted from Fig.io spec

import { Subcommand, Option, Argument } from '../../types.js';

const completion = {
  name: "self-test",
  description: "Runs a self-test of the CLI"
};

export default completion;
