// az init completion spec for CLIFlow
// Auto-converted from Fig.io spec

import { Subcommand, Option, Argument } from '../../types.js';

const completion = {
  name: "init",
  description: "It's an effortless setting up tool for configs"
};

export default completion;
